# Self-Mix Interferometry Tasks

---

## Infrastructure & Data Pipeline

- [x] **Rename project repo to "smi"** - avoid underscore vs hyphen confusion in repo packages, use more conventional project name; requires renaming remote Github repo.
- [x] **Bake normalization stats of input/output into nn.Module** - Instead of computing normalization stats in the VelocityDataset, let's write a script that points at a dataset, computes the normalization statistics for each input/output feature generated by the FeatureRegistry (see below) and outputs them to a Python file for version control. Each model should then take these parameters from the features normalization params Python file and bake them into register_buffers used by the input/output heads to the model for scaling of the inputs and outputs. This way, the internal layers of the model only see mean-zero and variance scaled input and output features, but in the rest of the code base we default to working with the raw param values. We can set this up as an abstract base class called Model(nn.Module) that only wraps the internal model (one of the models currently in analysis/models/) in this input/output normalization scaling.
- [x] **Evaluate data loading efficiency** - Is HDF5 the most efficient data storage format? Can we optimize storage space or loading speed using compression or binary formats (parquet format)? We want best speed for training and for EDA and also minimize disk storage if there is minimal impact on loading speed.
- [x] **Setup usage of Lightning Datamodules** - reduce boilerplate Dataloader code in datasets.py and training_interface.py.
- [x] **Setup feature registry using Polars** Based on example in Documents/Projects/coffee repo; use Polars pl.Expr lazy eval for complex feature engineering through a FeatureRegistry evaluated by Dataset. For now, we don't have any complex feature engineering to do, but we want the infrastructure in place for the flexible "define a new feature in features.py, decorate with "register_feature" wrapper to auto-add to default feature registry, run training with new feature"
    - [x] **Bake FeatureRegistry as an input head into each model** Is there a flexible way to bake the input feature definitions from the default FeatureRegistry into a PyTorch nn.Module? We would like to be able to package our trained models as compiled TorchScript that takes raw device outputs as the first inputs to the model - these pass through the normalization wrapper and would, ideally, then pass through a FeatureMap layer that performs the same feature engineering (if we end up defining any) that FeatureRegistry performs. This would also make data loading during training easier, as the Dataloader/Dataset would not need to directly use the FeatureRegistry to compute inputs and serve these to the model, the model would use the FeatureMap implicitly during training. The advantage of the FeatureRegistry is mainly the use of Polars during EDA - but this alone is intrinsically (I think) not compatible with TorchScript compiled feature definitions in a FeatureMap layer baked into the model in the same way we want to bake the input/output normalization in to each model.
    - [x] **Check that existing models can compile to TorchScript for packaging** Apply simple refactors if not unless doing so breaks the model design in a significant way as it is currently defined in analysis/models/. We should bake in a sanity check to the training pipeline that whatever model we are training 1. can be compiled to TorchScript and 2. should be compiled to TorchScript for faster training (is this true, or do we only want to compile the trained model to TorchScript at the end of training?)
    - [x] **Move all features related definition/manipulation to a separate "features" directory** this would include our normalization computations. The normalization wrapper/base class nn.Module should be in the models/ directory.
- [ ] **Finish setting up synthetic data pipeline** We are in the process of setting up a synthetic data pipeline to 1. rapidly generate synthetic data directly on GPU for fast training for the purpose of 2. rapid evaluation of models and feature engineering.
    - [x] **Mixture of experimental acquisition and simulation code needs separation of concerns** There is currently a mixture data acquisition from experiment and simulation code in the acquisition/ directory. We should separate these so that all of the packages in acquisition/simulations/ are promoted to a top-level synthetic/ directory. The RedPitaya device management and data acquisition code can then go in a top-level redpitaya directory, eliminating the acquisition/ directory that is confusingly grouping these two modules (the RedPitaya driver code does need some code from the models in simulations/ but the simulations themselves are not intrinsically belonging to the concept of data acquisition). We will use the code in the new synthetic directory, and expand upon it, for the synthetic data pipeline.
- [ ] **Establish consistent units convention** -- loss is currently logged in um/ms but model learns um/s; pick one and enforce throughout. This has already caused one near-miss: the baseline report tabulated velocity RMSE in um/s and claimed it was directly comparable to the network's, which would have been off by 1000x (`LitModule.loss_function` scales velocity by 1e-3 before the MSE). Caught in review, but the ambiguity is still live -- a single conversion at the dataset boundary, or a named unit on every velocity variable, would make it impossible.
- [ ] **Investigate whether using Ray Tune fractional device allocation** can help with synthetic data parameter sweeps. Generating synthetic data on GPU is fast and we can generate small amounts of data at the same time, so we could use Ray to train many models at once with different hyperparameters and select the best one.
- [ ] **Check for signal delay offset between velocity and PD channels** -- if large enough, offset corrupts supervision signal and degrades prediction quality
- [ ] **Continuous data acquisition via Julia / Red Pitaya DAQ Server** -- evaluate whether continuous acquisition is worth porting to Julia: [RedPitayaDAQServer docs](https://tknopp.github.io/RedPitayaDAQServer/dev/scpi.html)
- [x] **GPU data loading** Have we been bottlenecked this entire time using dataloaders on a dataset that we could have loaded directly into GPU memory and sliced? **Answered: no.** Training is GPU bound. Every config with `num_workers >= 4` sits within 3% of the model-only ceiling (~212 samples/s) while the loader alone supplies ~1573-1600 samples/s, roughly 7.5-9x headroom; full GPU residency buys 1.7%, inside the noise. The per-sample duplicate FFT was real and is fixed (53% faster loader in isolation, no end-to-end change). `GPUResidentDataset` exists as an opt-in mode for determinism and worker-free debugging, not for speed. See `smi/analysis/benchmark_loading.py`; if throughput matters later the lever is the model side (AMP, channels_last, torch.compile, batch size).

---

## Model Architecture

- [ ] **Remove conditional downsampling in `TemporalBlock`** -- always route through MLP; also evaluate whether stacking two same-channel temporal blocks in a row is useful or whether every block should vary channel count
- [ ] **Correct `receptive_field` calculation** -- current formula does not account for the fact that each `TemporalBlock` contains TWO convolutional layers
- [ ] **Model baselines** Compare TCNs to simpler models to justify the expense of the modeling approach. How well do an MLP and LightGBM tree perform? (The *non-learned* baseline is done -- see "Establishing baselines" below. This item is the learned-but-simpler comparison, still open.)

---

## Experiments & Benchmarks

- [ ] **EDA feasibility study** -- discuss with Gemini: is the desired accuracy achievable at all from the raw signal statistics, or is training on idealized synthetic data the best path?
- [ ] **Histogram analysis plots** -- prediction vs actual, gradient value vs signal value, gradient value vs velocity, gradient value vs displacement
- [ ] **Baseline: SCNN vs TCN vs Barland at matched parameter count** -- direct apples-to-apples comparison
- [ ] **SCNN / TCN receptive field study** -- sweep receptive field size and lookahead window; quantify effect on mmfiber/freespace generalization gap
- [ ] **Feature engineering for fiber/freespace gap** -- investigate whether any simple hand-crafted features dramatically close the mmfiber/freespace distribution shift
- [ ] **Exclude OOD amplitude frames from dataset** -- identify and count bad frames with raw signal amplitude out of distribution; assess whether they hurt training or force the model to exploit multi-channel information

---

## Analysis & Presentation

- [x] **Establishing baselines** You DO actually need to check how accurate you would get just fitting the data with least squares or some other simple parametric method. You can do this via an MSE on the data given the model (likelihood of the data). **Done** -- `smi/analysis/baseline_fit.py`, report at `report/baseline.typ`. Likelihood and MSE turn out to be the same computation under Gaussian residuals, so the closed-form fit is both the simple and the rigorous option. Baseline (N=200): displacement RMSE 0.433 um free space / 0.462 um fiber, velocity RMSE 488 / 550 um/s -- which is 0.488 / 0.550 **um/ms**, the unit `LitModule` actually logs the velocity loss in (it scales by 1e-3 before the MSE). Compare against a training log in um/ms or the comparison is off by 1000x; see the units-convention task above. But the forward-model residual says the Michelson model explains only 9-38% of the variance *with displacement known*, so the baseline's error is model/data mismatch rather than a weak estimator -- the same decoder handles synthetic Michelson fringes ~20x better. Follow-up below.
    - [ ] **Why does the Michelson model fit this data so badly?** Model fit collapses at large drive excursion: R^2 falls to 0.03-0.11 above 2 um peak-to-peak in every channel, against 0.44-0.85 in the smallest bin. Bandwidth was informally ruled out (max fringe rate ~7.4 kHz, well inside band) but that check is not committed. Speaker nonlinearity, coil-driver calibration error at large drive, or fringe-rate-dependent detector response are the candidates. This bounds every method, learned or not, so it is worth more than tuning the inverse.
- [ ] **Cavity reflectance notebook: Fisher information** -- enhance notebook to compute Fisher information and characterize the dynamic range / FI tradeoff
- [ ] **Block diagram visualization of network** -- set up a clean block diagram rendering of the model architecture

---

## Hardware

- [ ] **Dichroic reflector for reference wavelength to deconvolve phase disruptions from fiber** Might be simpler passive approach to removing phase distortion from the fiber compared to modulation techniques. Test in simulation first.
- [ ] **PCB design for current supply / active modulation** -- evaluate whether a custom PCB improves sensing performance and scales to more diodes; find an optics hardware expert to consult
